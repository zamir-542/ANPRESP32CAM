# Personal data — captured plate imagery and the read log. Never commit.
phone/captures/
phone/reads.db
*.db

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local settings (personal, not shared)
.claude/

# Internal planning/context docs — kept locally to continue work, never pushed.
context/
